import { OnChanges, OnInit, ElementRef, SimpleChanges } from '@angular/core';
import { CesiumService } from '../../services/cesium/cesium.service';
export declare class AcMapComponent implements OnChanges, OnInit {
    private _cesiumService;
    private _elemRef;
    private document;
    private static readonly DEFAULT_MINIMUM_ZOOM;
    private static readonly DEFAULT_MAXIMUM_ZOOM;
    private static readonly DEFAULT_TILT_ENABLE;
    minimumZoom: number;
    maximumZoom: number;
    enableTilt: boolean;
    flyTo: any;
    constructor(_cesiumService: CesiumService, _elemRef: ElementRef, document: any);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
