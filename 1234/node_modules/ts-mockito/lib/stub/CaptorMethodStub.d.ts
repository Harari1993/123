import { Matcher } from '../matcher/type/Matcher';
import { MethodStub } from './MethodStub';
import { Captor } from '../Captor';
export declare class CaptorMethodStub implements MethodStub {
    private matchers;
    private captors;
    private validator;
    constructor(matchers: Array<Matcher>, captors: Captor<any>[]);
    isApplicable(args: any[]): boolean;
    execute(args: any[]): void;
    getValue(): any;
}
